fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-multipart>=0.0.12
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
# Tests / dev
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
aiosqlite>=0.20
# Cliente WebSocket para STT en streaming (Gladia v2 Live).
websockets>=13
# Integraciones MVP (Gladia STT + Mistral LLM)
# Fijado a la línea 2.x: el provider usa el layout `mistralai.client` y
# `chat.parse_async(response_format=...)` (salida estructurada), verificado con 2.5.
mistralai>=2.5,<3
# Integraciones futuras:
# fhir.resources>=7.1
# speechmatics-python
# celery[redis]>=5.4
